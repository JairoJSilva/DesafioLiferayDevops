apiVersion: v1
kind: Service
metadata:
  name: liferay-app-service
  namespace:: liferay
spec:
  selector:
    app: liferay-app-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort